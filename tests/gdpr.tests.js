import {chai, assert} from 'meteor/practicalmeteor:chai';
import {PublicationCollector} from 'meteor/johanbrook:publication-collector';


describe('User consent', function () {
    it('should display user consent requirement if the user has not already consented to', function () {

    });
    it('should not display user consent requirement if the user has already consented to', function () {

    });
    it('should ask to tick the box on user consent validation', function () {

    });
    it('should register user consent on validation', function () {

    });
});

describe('User data access', function () {
    it('Meteor.user().gdpr data should be available after subscription', function () {
    });
});

describe('Profile Name component', function () {
    it('should display name and picture', function () {

    });
    it('when clicked, should display the Profile Window', function () {

    });
});

describe('Contact Form component', function () {
    it('The modal should display when clicking on the contact button', function () {

    });
    it('Email should send upon validation', function () {

    });
});
