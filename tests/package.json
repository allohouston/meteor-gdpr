{
    "name": "tests",
    "private": true,
    "scripts": {
        "test": "METEOR_PACKAGE_DIRS=../../ meteor test --once --driver-package meteortesting:mocha",
        "test:watch": "METEOR_PACKAGE_DIRS=../../ TEST_WATCH=1 meteor test --driver-package meteortesting:mocha"
    },
    "dependencies": {
        "@babel/runtime": "^7.0.0-beta.42",
        "meteor-node-stubs": "^0.3.2",
        "simpl-schema": "1.4.3"
    }
}
